spring:
  application:
    name: service
  threads:
    virtual:
      enabled: off
server:
  port: 8081

management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: always
  endpoints:
    web:
      exposure:
        include: health, info