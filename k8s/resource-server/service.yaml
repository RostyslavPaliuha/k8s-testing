apiVersion: v1
kind: Service
metadata:
  name: service
  namespace: service-ns
  labels:
    app: service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8081
      name: http
  selector:
    app: service
