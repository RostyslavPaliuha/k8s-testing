apiVersion: v1
kind: Service
metadata:
  name: ingress-gateway
  namespace: ingress-gateway-ns
  labels:
    app: ingress-gateway
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      name: http
  selector:
    app: ingress-gateway
