apiVersion: v1
kind: Service
metadata:
  name: discovery-server
  namespace: discovery-server-ns
  labels:
    app: discovery-server
spec:
  type: ClusterIP
  ports:
    - port: 8500
      targetPort: 8500
      name: http
  selector:
    app: discovery-server
